{
	"$schema": "node_modules/wrangler/config-schema.json",
	"name": "web-template",
	"main": ".open-next/worker.js",
	"compatibility_date": "2025-10-08",
	"compatibility_flags": ["nodejs_compat", "global_fetch_strictly_public"],
	"assets": {
		"binding": "ASSETS",
		"directory": ".open-next/assets"
	},
	"observability": {
		"enabled": true
	},
	"upload_source_maps": true,
	"d1_databases": [
		{
			"binding": "DB",
			"database_name": "web-template-local-db",
			"database_id": "local",
			"migrations_dir": "drizzle"
		},
		{
			"binding": "NEXT_TAG_CACHE_D1",
			"database_name": "web-template-local-db",
			"database_id": "local"
		}
	],
	"r2_buckets": [
		{ "binding": "ASSETS_BUCKET", "bucket_name": "web-template-local-assets" },
		{ "binding": "NEXT_INC_CACHE_R2_BUCKET", "bucket_name": "web-template-local-cache" }
	],
	// Local dev queue producer (no-op in local, but prevents runtime binding errors)
	"queues": {
		"producers": [{ "binding": "EMAIL_QUEUE", "queue": "web-template-local-email-queue" }]
	},
	// NOTE: Queue consumers are configured per environment below, but OpenNext may not
	// support queue consumers natively. In that case, a thin wrapper worker is needed
	// to call handleEmailQueue() from src/lib/queue-consumer.ts.
	"env": {
		"uat": {
			"name": "web-template-uat",
			"vars": { "ENVIRONMENT": "uat" },
			"d1_databases": [
				{
					"binding": "DB",
					"database_name": "web-template-uat-db",
					"database_id": "<from terraform output>",
					"migrations_dir": "drizzle"
				},
				{
					"binding": "NEXT_TAG_CACHE_D1",
					"database_name": "web-template-uat-db",
					"database_id": "<from terraform output>"
				}
			],
			"r2_buckets": [
				{ "binding": "ASSETS_BUCKET", "bucket_name": "web-template-uat-assets" },
				{ "binding": "NEXT_INC_CACHE_R2_BUCKET", "bucket_name": "web-template-uat-cache" }
			],
			"queues": {
				"producers": [{ "binding": "EMAIL_QUEUE", "queue": "web-template-uat-email-queue" }],
				"consumers": [{ "queue": "web-template-uat-email-queue", "dead_letter_queue": "web-template-uat-email-dlq", "max_retries": 3 }]
			}
		},
		"prod": {
			"name": "web-template",
			"vars": { "ENVIRONMENT": "prod" },
			"d1_databases": [
				{
					"binding": "DB",
					"database_name": "web-template-prod-db",
					"database_id": "<from terraform output>",
					"migrations_dir": "drizzle"
				},
				{
					"binding": "NEXT_TAG_CACHE_D1",
					"database_name": "web-template-prod-db",
					"database_id": "<from terraform output>"
				}
			],
			"r2_buckets": [
				{ "binding": "ASSETS_BUCKET", "bucket_name": "web-template-prod-assets" },
				{ "binding": "NEXT_INC_CACHE_R2_BUCKET", "bucket_name": "web-template-prod-cache" }
			],
			"queues": {
				"producers": [{ "binding": "EMAIL_QUEUE", "queue": "web-template-prod-email-queue" }],
				"consumers": [{ "queue": "web-template-prod-email-queue", "dead_letter_queue": "web-template-prod-email-dlq", "max_retries": 3 }]
			}
		}
	}
}
